fm.api.project:
    resource: '@FMProjectBundle/Controller/Api/Rest/ProjectController.php'
    type: rest
    prefix: api/rest/{version}/
    requirements:
        version: latest|v1
        _format:  json
    defaults:
        version: latest
